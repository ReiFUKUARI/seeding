# フェーズ4：環境構築 作業報告

要件定義書「次のステップ（フェーズ4：環境構築）」の4項目に対する成果と、
**この作業環境では実施できなかったこと**をまとめる。

---

## サマリー

| # | タスク | 状態 |
|---|---|---|
| ① | .NET + WPF プロジェクトの雛形作成 | 構成完了。`SmtpProbe`・`Core` は **Windows実機でのビルド成功を確認済み**（WPF本体は未検証） |
| ② | Microsoft.Data.Sqlite によるDBスキーマ初版構築 | **完了・検証済み（17/17パス）** |
| ③ | MailKit による WebARENA 実アカウントへの疎通検証 | **完了**。587 + Auto/StartTls とも接続・認証・送信に成功。重要な発見あり（下記） |
| ④ | MSIXパッケージングの検討 | 完了（構成ファイル＋設計書。WPF本体を含むビルドは未検証） |

### 実施できなかったことと理由

作業環境（Linux コンテナ）には **.NET SDK が入っておらず、
その取得元（`builds.dotnet.microsoft.com`）へのネットワークアクセスも
遮断されている**ため、以下は実行できていません。

- **ビルド・実行**：`dotnet build` / `dotnet run` を一度も実行していません。
  そもそも WPF は Windows でしかビルドできないため、いずれにせよ実機での確認が必要です。
- **NuGet パッケージの復元**：`dotnet restore` は実行できていません。
  ただし nuget.org へは到達できたため、記載した各バージョンが実在すること、
  および `net10.0` と互換のターゲットを持つことは確認済みです
  （MailKit は net10.0 向けアセンブリを同梱する 4.17.0 を採用）。
  依存関係の完全な解決は実機での復元時に確認してください。
- **③ の疎通検証本体**：WebARENA の実アカウント情報がなく、
  かつサンドボックスから外部SMTPへ接続できません。

### Windows実機での検証結果（確定）

Windows実機で以下を確認いただきました。

- `dotnet build src\MailDeliveryTool.SmtpProbe\MailDeliveryTool.SmtpProbe.csproj -c Release`
  → **成功**（プロジェクト参照経由で `Core` も含めてビルドされる）
- `smtp-probe` による WebARENA 実アカウント（`dc30.etius.jp:587`）への疎通検証
  → **`Auto` / `StartTls` いずれも接続・認証・テストメール送信のすべてに成功**
  （TLS 1.2 / AES256、認証は `CRAM-MD5` が自動選択された）

ビルド時に2件の警告が出たため修正済み：

- `Core` プロジェクトで `CsvHelper` / `System.Text.Encoding.CodePages` を
  未使用のまま参照していた（CSV機能はフェーズ5で実装予定のため、
  実装時に `PackageReference` を追加する形に変更）
- MailKit 4.17.0 で `SslCipherAlgorithm` / `SslCipherStrength` が非推奨化されていた
  （.NET の `SslStream` 側の同等プロパティの非推奨化に追随したもの）。
  単一の暗号スイート識別子を返す `SslCipherSuite` に置き換えた

WPF本体（`MailDeliveryTool.App`）とMSIX（`.wapproj`）のビルドはまだ確認できていません。

### 重要な発見：サーバーのメールサイズ上限は20MB（要件定義書の想定25MBと異なる）

疎通検証で判明した最大の成果です。**要件定義書 3章「サーバー側上限25MB」は誤り**で、
実測は **20.0MB** でした。

モックの添付ファイル仮値（20MB。要件定義書14章）は「添付ファイルの生サイズ」の
つもりでしたが、サーバーの20MBは**Base64エンコード後を含むメール全体の上限**です。
つまりモックの仮値をそのまま実装すると、20MBの添付ファイルはエンコードで
約28MBに膨れ、**必ずサーバー側のサイズ超過エラーになります**。

添付ファイル合計の実装上限は **13MB** を推奨します（20.0MB ÷ 1.4 ≒ 14.3MB から、
本文・ヘッダ分の余裕を見て切り下げ）。詳細と根拠は
[D-007](./decisions.md#d-007-添付ファイル合計の上限は13mb暫定要最終承認)。
ユーザーに見せる数値のため、**最終確定は承認をお願いします**。

**【要件定義書の改訂が必要】** 3章の「サーバー側上限25MB」を「20MB（実測値）」に。

なお、**ソースコードを書いて終わりにはせず、この環境で検証できるものは検証しました**。

- DBスキーマ：実際の SQLite エンジンに適用し、要件レベルの振る舞いを17項目検証（全パス）
- プロトコルログのマスク処理：4種の認証フローに対するテストで、
  当初の実装が CRLF 改行時にパスワードをマスクし損ねる不具合を発見し修正
- NuGet パッケージ：全6パッケージが nuget.org 上に実在し、`net10.0` と
  互換のターゲットを持つことを確認
- MailKit の API：コードで使用している型・メンバー23件が MailKit 4.17.0 の
  アセンブリに存在することを確認（名前レベルの確認であり、
  引数の型まで含めた検証はコンパイル時に行われます）
- 全 XML / XAML の妥当性、XAML のリソースキーとコードビハインドの対応関係

---

## ① プロジェクト雛形

```
MailDeliveryTool.sln
├── src/MailDeliveryTool.Core/        業務ロジック（UI非依存）
│   ├── Data/                          SQLite 接続・スキーマ・初期化
│   ├── Mail/                          MailKit による SMTP 処理
│   ├── Models/                        ドメインモデル
│   └── Security/                      DPAPI による暗号化
├── src/MailDeliveryTool.App/         WPF 本体
├── src/MailDeliveryTool.SmtpProbe/   SMTP疎通検証ツール（配布対象外）
└── packaging/MailDeliveryTool.Package/  MSIX パッケージング
```

UI から独立した `Core` を分けているのは、SMTP 疎通検証ツールから
同じ接続処理を呼べるようにするためです。検証で使ったコードが
そのまま本番の送信処理になるため、「検証は通ったのに本番で動かない」を避けられます。

### ターゲットフレームワーク：.NET 10（決定済み）

`Directory.Build.props` で **.NET 10（`net10.0-windows`）** を指定している。

要件定義書 12章は .NET 8/9 を挙げているが、いずれもサポート期限が
近い、または切れているため、協議のうえ .NET 10 を採用した。

| バージョン | 区分 | サポート終了 |
|---|---|---|
| .NET 8 | LTS | 2026年11月 |
| .NET 9 | STS | 2026年5月（終了済み） |
| **.NET 10** | **LTS** | **2028年11月** |

.NET 8 で着手するとフェーズ5〜6の開発期間中にサポートが切れる見込みであり、
新規開発である以上 LTS の .NET 10 で始めるのが妥当と判断した。

> 要件定義書 12章の記載（.NET 8/9）は、この決定に合わせて改訂が必要。

将来のバージョン移行も `Directory.Build.props` の
`WindowsTargetFramework` 1行と `global.json` の書き換えで済むよう構成している。

---

## ② DBスキーマ初版

要求どおり4種のテーブルを構築しました。

- 宛先（`Contact`）
- カテゴリ軸（`CategoryAxis`）・カテゴリ値（`CategoryValue`）
- 中間テーブル（`ContactCategoryValue`）
- メールアカウント設定（`MailAccountSetting`）

加えて `AppSetting`（署名・バックアップ設定）と `SchemaVersion`
（マイグレーション管理）を用意しています。

設計判断の詳細は [`db-schema.md`](./db-schema.md) を参照。特に以下は
要件を読み込んだうえでの意図的な判断です。

- **会社名・担当者名に空文字禁止の制約を付けていない**
  → 7.1 の送信前検証が「空欄の宛先が存在しうる」前提のため
- **メールアドレスに UNIQUE を付けていない**
  → 4章「重複メールアドレスはそのまま重複送信を許容」のため
- **送信履歴テーブルを作っていない** → 9章「DB化は不要」のため

検証:

```bash
$ python3 tools/validate_schema.py
...
17/17 passed  (SQLite 3.45.1)
```

---

## ③ MailKit 疎通検証

### 成果物

`smtp-probe`（`src/MailDeliveryTool.SmtpProbe`）— 587 + `SecureSocketOptions.Auto`
で接続し、以下を報告するコンソールツール。

- **STARTTLS が実際にネゴシエートされたか**（要件定義書 14章の未検証事項）
- サーバーが広告した認証方式と、認証の成否
- サーバーが `SIZE` 拡張で広告するメールサイズ上限
- 認証情報をマスクした SMTP プロトコルログ全文

### 要件定義書 14章「パケットキャプチャ等で検証」について

**パケットキャプチャは不要です。** MailKit の `SmtpClient.IsSecure` を
読めば、STARTTLS が実際にネゴシエートされたかを直接判定できます。
`Auto` を指定したうえで `IsSecure == true` なら、STARTTLS が使われた証拠になります。
Wireshark を用意する必要はありません。

### セキュリティ上の配慮

- パスワードは**コマンドライン引数で受け取らず対話入力のみ**
  （コマンド履歴・プロセス一覧に残さないため）
- プロトコルログの `AUTH` 行と認証チャレンジ応答は自動マスク
  （AUTH PLAIN / AUTH LOGIN の2チャレンジ / CRAM-MD5 の各フローでテスト済み）

### 未実施

**実際の疎通検証は未実施です。** 手順は
[`smtp-verification.md`](./smtp-verification.md) にまとめてあり、
結果の記入欄も用意しています。Windows 実機での実施をお願いします。

---

## ④ MSIXパッケージング

`.wapproj`（Windows Application Packaging Project）方式を採用し、
構成ファイル一式を `packaging/MailDeliveryTool.Package/` に配置しました。
検討内容は [`msix-packaging.md`](./msix-packaging.md) を参照。

特に重要な発見として、**MSIX ではアンインストール時にアプリデータが
削除される**ため、SQLite DB に入っている宛先リストが消えます。
バックアップ先を（パッケージ管理外の）ドキュメントフォルダにしている
要件定義書 5.5 の設計はこの点でも妥当で、`AppPaths` にその意図を
実装済みです。運用手順書に「再インストール前にバックアップ」を
明記する必要があります。

社内確認が必要な未決事項（署名方式・Publisher名・自動更新の要否・
アプリアイコン）は同文書の「6. 未決事項」に一覧化しています。

---

## 次のアクション

### 依頼したいこと

1. **添付ファイル上限13MBの最終承認**（[D-007](./decisions.md#d-007-添付ファイル合計の上限は13mb暫定要最終承認)）
2. **WPF本体（`MailDeliveryTool.App`）の再検証** — 直前のLinux環境でのビルドは
   `NETSDK1100`（Windows以外でのビルドに未対応）で失敗したため、
   `Directory.Build.props` に `EnableWindowsTargeting`（Windows以外でのみ有効化、
   Windows実機のビルドには無影響）を追加した。以下の両方を確認してほしい：
   - Windows以外の環境: `dotnet build src/MailDeliveryTool.App/MailDeliveryTool.App.csproj -c Release`
     でコンパイルが通るか（**実行・起動確認はできない**。WPFはWindowsのUIランタイムが必要）
   - **Windows実機**: 同じビルドに加えて `dotnet run --project src\MailDeliveryTool.App` で
     実際にウィンドウが起動し、フェーズ4の診断表示（DB初期化状況）が出るか
3. MSIX（`.wapproj`）のビルド確認（Windows + MSBuild または Visual Studio。
   [手順](./msix-packaging.md#5-ビルド手順)）
4. MSIX 未決事項の社内確認（[一覧](./msix-packaging.md#6-未決事項社内確認が必要)）

### 確定済みの仕様判断

実装前に潰しておくべき論点は [決定事項ログ](./decisions.md) に集約した。
フェーズ5の実装はこの内容に従うこと。

| ID | 内容 |
|---|---|
| D-001 | ターゲットフレームワークは .NET 10（LTS） |
| D-002 | カテゴリ値の「値名の変更」は実装しない |
| D-003 | カテゴリ値の追加・削除はモーダルにも即時反映する |
| D-004 | 週次バックアップは「起動時に前回から7日経過していたら実行」 |
| D-005 | 送信中はウィンドウの閉じるボタンを無効化する |
| D-006 | CSVエクスポートに停止中の宛先も含める（列追加の要否は未決） |
| D-007 | 添付ファイル合計の上限は13MB（サーバー実測20MBから算出・要最終承認） |

D-001・D-006・D-007 は **要件定義書本体の改訂が必要**。

### フェーズ5へ持ち越す検証項目（要件定義書 14章）

- 送信ペーステスト（75通/分での安定性）— 送信機能の実装が前提のため
- 添付ファイルの実容量上限の確定 — ③の `SIZE` 広告値を根拠に決定する
